function [x, y] = load_dataset(filename)

end


function [X, Y] = load_all_datasets(filenames)

end

function filenames = get_datasets(directory)

end

function [band_amp, band_phase] = band_hilbert(x, band)
%bandpass
%Hilbert amplitude at beta

end

function threshold(x)

end

function amp_at_t(x, t, fs)

end

